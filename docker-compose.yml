version: "3.8"

services:
  spending-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    image: spending-tracker:latest
    container_name: spending-tracker
    restart: unless-stopped
    environment:
      - TZ=Asia/Singapore
    env_file:
      - .env
    networks:
      - spending-tracker-net

networks:
  spending-tracker-net:
    external: true